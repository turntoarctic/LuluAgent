<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 解决 Android 11+ 软件包可见性限制：允许发现并拉起 Boss 直聘 -->
    <queries>
        <package android:name="com.hpbr.bosszhipin" />
    </queries>

    <!-- 基础网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 悬浮窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 前台服务与电池白名单权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".LuluApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- 宿主总控大厅 -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- API Key 配置页 -->
        <activity
            android:name=".ui.settings.ApiKeyConfigActivity"
            android:exported="false"
            android:label="DeepSeek 凭证配置" />

        <!-- 个人简历编辑页 -->
        <activity
            android:name=".ui.settings.ResumeEditorActivity"
            android:exported="false"
            android:label="个人简历与背景设定" />

        <!-- 筛选与评分策略配置页 -->
        <activity
            android:name=".ui.settings.FilterSettingsActivity"
            android:exported="false"
            android:label="筛选与评分策略" />

        <!-- 投递与评估历史看板 -->
        <activity
            android:name=".ui.dashboard.HistoryRecordActivity"
            android:exported="false"
            android:label="岗位投递流水" />

        <!-- 无障碍服务核心通道 -->
        <service
            android:name=".accessibility.BossAccessibilityService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- 双模悬浮交互前台服务 -->
        <service
            android:name=".floating.FloatingHUDService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Floating HUD dashboard and automation controller for job hunting agent" />
        </service>

    </application>

</manifest>
